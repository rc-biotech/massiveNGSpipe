% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_list_files.R
\name{list_files_pipelines}
\alias{list_files_pipelines}
\title{List existing files for one massiveNGSpipe pipeline step, across all pipelines}
\usage{
list_files_pipelines(pipelines, config, step, full_names = TRUE)
}
\arguments{
\item{pipelines}{the pipelines list (see \link{pipeline_init_all})}

\item{config}{the mNGSp config object (see \link{pipeline_config})}

\item{step}{character, one of "start", "fetch", "trim", "collapsed",
"contam", "aligned", "cleanbam", "exp", "ofst", "cigar_collapse",
"pshifted", "covrle", "bigwig", "valid_pshift", "merged_lib", "pcounts".}

\item{full_names}{logical, default TRUE. If FALSE, returns basenames only.}
}
\value{
a named list, one element per experiment (\code{"<accession>-<organism>"}),
each a character vector of existing file paths for that step
(\code{character(0)} if none exist yet, e.g. the step hasn't run for that
experiment). For \code{step = "exp"}, the "file" is a stand-in: the experiment
name itself if its ORFik experiment record exists, since an ORFik
experiment isn't stored as one single file on disk.
}
\description{
A single, standardized entry point for "which studies/experiments have
files for step X", instead of checking flags alone or re-deriving each
step's file location by hand. Read-only: never downloads, creates, or
deletes anything.
}
\examples{
\dontrun{
config <- pipeline_config()
pipelines <- pipeline_init_all(config)
list_files_pipelines(pipelines, config, "collapsed")
# Which studies have any collapsed files at all:
has_collapsed <- vapply(list_files_pipelines(pipelines, config, "collapsed"),
                        function(x) length(x) > 0, logical(1))
}
}
